const notifications_in_dropdown = 20;
const requests_in_dropdown = 20;
const domain = 'http://iitbaba.com';

module.exports = {
    notifications : notifications_in_dropdown,
    requests : requests_in_dropdown,
    domain : domain
};