extends ../layout

block content
  .container.avg
    .row.row-hiw
      .col-lg-6.col-sm-12.col-hiw
        h1.hiw-title How it works
        p You estimate how much your friends will earn when they graduate.
        p They estimate for you.
        p Unit for prediction is Lakh INR/year everywhere.
        p By PageRank algorithm everyone's worth is calculated. Know how.
        p Start with estimating yourself when you graduate.
        form#prediction(method='POST')
          input(type='hidden', name='_csrf', value=_csrf)
          input.form-control.predict-yourself-input(type='number', step='0.01', placeholder="Lakh INR/year",name='babaSalary', id='babaSalary', autofocus, required)
          .row.predict-yourself-row
            .predict-yourself
              button.btn.btn-primary.btn-reset.black-theme-bg(type='submit') Predict
    script.
      $("#prediction").submit(function() {
          console.log("This");
          console.log(sal);
          console.log("That");
          if($("#salary").val()>20){
              alert("Are you high? Give an aprropriate guess.");
              return false;
          } 
          if($("#salary").val()<2){
              alert("Do yo do cheap durgs? Try something else.");
              return false;
          }
          return true;
      });