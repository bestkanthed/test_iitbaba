extends ../layout

block content
    .container.login
      form.form-horizontal.login(method='POST')
        legend Login
        input(type='hidden', name='_csrf', value=_csrf)
        .form-group
          label.col-sm-3.control-label(for='username') Username
          .col-sm-7
            input.form-control(type='username', name='username', id='username', autofocus, required)
        .form-group
          label.col-sm-3.control-label(for='password') Password
          .col-sm-7
            input.form-control(type='password', name='password', id='password', required)
        .form-group
          .col-sm-offset-3.col-sm-7
            button.col-sm-3.btn.btn-primary.black-theme-bg(type='submit')
              i.fa.fa-user
              |  Login
            a.btn.btn-link.black-theme(href='/forgot') Forgot your password?
            a.btn.btn-link.black-theme(href='/auth/iitbsso') Register?